# Ignore all files by default, but scan all directories
*
!*/

# Do not ignore git files in the root of the repo
!/.git*

# Do not ignore current project's .uproject
!/Game.uproject

# Do not ignore source and config dirs
!/Source/**
!/Config/**

# Only allow .uasset and .umap files from /Content 
# They're tracked by git-lfs, don't forget to track other
# files if adding them here.
!/Content/**/*.uasset
!/Content/**/*.umap

# OS/platform generated files

# Windows
ehthumbs.db
Thumbs.db

# Mac OS X
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*

# Linux
*~
.directory

# vim
[._]*.s[a-w][a-z]
[._]s[a-w][a-z]
*.un~
Session.vim
.netrwhist
